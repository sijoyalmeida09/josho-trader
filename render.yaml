services:
  - type: web
    name: josho-trader
    runtime: python
    buildCommand: pip install -r requirements.txt fastapi uvicorn jinja2
    startCommand: uvicorn dashboard.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
